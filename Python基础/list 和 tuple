List

list 是一个可变长度的有序列表
查找：可通过索引值查找 list[0], 一种查找方式为最后一个为-1，从后向前查，此时最后一个为-1，依次向前-2...

插入：使用insert(索引值, 需替换的内容)在指定索引位置插入，使用append在最后追加；

修改：直接使用list[索引值] = 替换值；

删除：使用pop(索引值) 删除指定索引位置的数据，不写索引值时表示删除最后一个。

注：list同时可以存储多种类型数据，不需统一。



tuple:

tuple 是一个不可变的有序列表，表示方式：t = (), 是的元组使用小括号表示。

tuple在初始化是需要给定值，因此是不可变的。同时可以初始化为空的tuple，并且当只包含一个元素时需要使用t = (1,),加逗号，以防和小括号运算产生分歧。

tuple 中元素的查找方式和list一样使用索引值，并且可以同时存储不同数据类型的数据。


注： 在使用时为了数据安全尽量使用tuple。

